# LAMP Vagrant for WordPress

## Setup
  1.  clone repo
  2.  unpack WordPress into `/html`
  3.  `$ echo "\"database_password"\" > .env
  4.  `$ vagrant up`
  5.  follow WordPress setup
  6.  create (copy/paste) the `wp-config.php` file
